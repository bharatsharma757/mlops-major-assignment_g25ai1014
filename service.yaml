apiVersion: v1
kind: Service
metadata:
  name: olivetti-service

spec:
  type: NodePort

  selector:
    app: olivetti-app

  ports:
  - protocol: TCP
    port: 5001
    targetPort: 5001
    nodePort: 30081
